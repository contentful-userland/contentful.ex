defmodule Contentful.Delivery.ContentTypes do
  @moduledoc """
  Provides functions around reading content types from spaces
  """

  # @spec list(String.t(), String.t(), String.t()) ::
  #         {:ok, list(ContentType.t())} | {:error, atom(), list(tuple())}
  # def list(content_type_id, space_id, api_key) do
  # end
end
